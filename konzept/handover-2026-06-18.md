# Handover — lingua-app, Session 2026-06-18

Kontext-Übergabe vor `/compact`. Resume-fähig aus kaltem Kontext.

## Was lingua-app ist
AI-Sprachtutor. **V1** = Streamlit + OpenRouter, BYOK-stateless, läuft produktiv.
**Entscheidung diese Session:** wird ein echtes Multi-Tenant-Produkt → **V2** (Next.js + Supabase + FastAPI). V1 bleibt unangetastet parallel.

- Repo: `miraculix95/lingua-app` (private), `~/cc-dev/lingua-app/`. Alles gepusht auf `main`.
- V1 live: `lingua.ai-devhub-247.site` · systemd-User-Unit `lingua-streamlit` · `127.0.0.1:8502`
- Tests: `.venv/bin/python -m pytest -q` (112 grün) · Lint: `.venv/bin/ruff check src/ tests/`
- Daemon-Restart (V1): `export XDG_RUNTIME_DIR=/run/user/1001; systemctl --user restart lingua-streamlit`

## Diese Session erledigt (alles committed + live verifiziert)
1. **Arabisch** als 9. Lern- + 8. UI-Sprache (RTL) + Tastatur-Eingabe-Hinweis (nicht-lateinische Sprachen) + „physische Tastatur"-Tipp.
2. **Transformation** (Gap #5), **Grammatik-Fokus auf Cloze** (#8), **Hörverständnis** (#2), **DELF-Schreiben** (#4), **CEFR-Einstufungstest** (#11) — neuer Block über dem Übungs-Dropdown, setzt Niveau automatisch.
3. **Einstufungstest-Fixes** (Feedback): Options-Shuffle gegen „immer A", Lösungen am Ende, Mix Grammatik/Vokabular/Leseverständnis, Scoring = Anzahl richtig (5/6→C1, nicht „längstes Präfix"), Reading-Items inhaltsbezogen (keine Niveau-Meta-Fragen), Streamlit-`level`-Apply-Bug via `_pending_level` gefixt.
3 DELF-Kompetenzen jetzt voll abgedeckt (Lesen/Hören/Schreiben). Production orale (#9) fehlt = V2/Voice.
4. **Favicon/Flagge** 🇫🇷 → 🗣️ (multi-language).
5. **App-weiter Jargon-Klartext-Audit:** Register→„Sprachstil/Style", LLM→„AI/KI/IA", Cloze(EN)→„Fill in the blanks", CEFR/QCM-Akronyme raus, FR „Compréhension"→„Lire/Écouter et comprendre", DELF erklärt statt vorausgesetzt. Zielgruppe: halb-alphabetisierte Migranten — Klartext statt Fachjargon.

## V2-Status (Epic TES-923)
**Konzept:** `~/cc-dev/lingua-app/konzept/architektur-v2.md` (ADR Option D, Datenmodell, Phasen 0–5).

**Entschieden:**
- Architektur: Next.js + **Supabase Auth + Supabase Postgres** (gleiches Projekt → RLS auf `auth.uid()`) + **FastAPI um V1-Python-Logik** (nicht in TS neuschreiben).
- Subdomain: **`lingua-saas.ai-devhub-247.site`** (eigene Produkt-Domain später).
- V2-Frontend kommt nach **neuem Verzeichnis `~/cc-dev/lingua-v2/`**. V1 NICHT anfassen.
- **Offen:** Kostenmodell (gratis/gefördert · Freemium · B2B) — blockt erst Phase 4; Architektur ist agnostisch (managed keys + usage-tracking ab Phase 0). BYOK ist tot (Zielgruppe hat keine Keys) → managed OPENROUTER_API_KEY serverseitig.

**Phase 0 FERTIG (commit d4e5dce):** FastAPI-Service `lingua-core` in `~/cc-dev/lingua-app/api/`, wrappt V1-`src/` (prompts/tasks/correction/delf/placement/vocab) hinter HTTP.
- Läuft als **systemd-User-Unit `lingua-core`** auf `127.0.0.1:8557` (Restart=on-failure, reboot-fest). Noch kein Caddy/Subdomain.
- 11 Endpunkte (1 pro Task-Shape): `/health` `/vocab/generate` `/generate/cloze` `/generate/transformation` `/correct` `/delf/task` `/delf/evaluate` `/reading/text` `/reading/questions` `/placement/test` `/placement/recommend`.
- E2E gegen echtes OpenRouter verifiziert (placement, recommend 5/6→C1, vocab→cloze).
- Smoke: `curl -sS http://127.0.0.1:8557/health`

## NÄCHSTER SCHRITT — Phase 1 (Resume hier)
Ziel: Next.js-App-Router-Scaffold in `~/cc-dev/lingua-v2/` + Supabase-Auth-Flow + **ein** Exercise-Type (Cloze) end-to-end gegen `lingua-core` (8557). *Verify:* eingeloggter Nutzer macht eine Cloze-Übung, Korrektur erscheint.

**Voraussetzung (Bastian muss liefern):** Supabase-Projekt (EU-Region) anlegen → Keys (URL, anon key, service_role) nach `~/cc-dev/lingua-v2/.env.local`. Claude kann Scaffold + Auth + Cloze-Page bauen, sobald die Keys da sind. context7 für Next.js/Supabase-Best-Practices vor dem Coden nutzen. shadcn fürs UI erwägen.

Danach: Phase 2 (restliche Exercise-Types + i18n portieren — die fehlenden API-Endpunkte writing/sentence/error/synonym/conjugation/dictation/listening sind mechanische Adds), Phase 3 (Postgres + SRS/FSRS + Fehler-Journal + Progress), Phase 4 (Kostenmodell), Phase 5 (Launch + Caddy/DNS für lingua-saas).

## Offene Nebenpunkte
- **TES-921:** Einstufungstest-Bank (geprüfte Tests speichern statt je Lauf per LLM generieren) — V1-Folge-Optimierung.
- Spanisch „Comprensión lectora/auditiva" im Jargon-Audit bewusst gelassen (Grenzfall) — auf Wunsch auf „Leer/Escuchar y comprender" umstellbar.
- V1-Repo hat untracked Altbestand (Screenshots, streamlit.log) — vor evtl. Public-Push aufräumen.

## Pointer
- Konzept: `konzept/architektur-v2.md` · Epic: Linear **TES-923** (lingua-app-Projekt) · Memory: `project_lingua_app_v2.md`
- API: `api/` (README, main.py, client.py) · V1-Logik: `src/` (unverändert wiederverwendet)
